const suhuRuangan = (TemperatureRuangan) => {
        return {
                dingin:Math.max(0, math.min(1,(25 - TemperatureRuangan)/ 5))
        }
};

const SuhuAC = (AC) => {
        Dingin:Math.max(0, Math.min(1, (20 - AC) / 4))
}

